{
  "name": "local-board",
  "version": "0.2.0",
  "description": "Repo-native Markdown ticket board and CLI for orchestrating local AI agent workflows.",
  "type": "module",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/NicholasMTElliott/local-board.git"
  },
  "keywords": [
    "kanban",
    "tickets",
    "board",
    "ai-agents",
    "workflow",
    "orchestration",
    "cli",
    "markdown"
  ],
  "bin": {
    "local-board": "./bin/local-board.js"
  },
  "files": [
    "bin",
    "src",
    "resources",
    "install.mjs",
    "SKILL.md",
    "SKILL_TEAM.md",
    "agents",
    "skills",
    "hooks"
  ],
  "scripts": {
    "check": "node --check bin/local-board.js && node --check src/active-steps.js && node --check src/cli.js && node --check src/codex-detect.js && node --check src/codex-dispatch.js && node --check src/config.js && node --check src/git.js && node --check src/install.js && node --check src/lock.js && node --check src/scaffold.js && node --check src/tickets.js && node --check src/worktrees.js && node --check install.mjs && node --check hooks/dispatch-ledger.js && node --check hooks/routing-validator.js && node --check hooks/evidence-gate.js && node --check hooks/approve-inline-consent.js",
    "test": "node --test",
    "validate": "node ./bin/local-board.js validate",
    "sync-resources": "node ./scripts/sync-resources.mjs",
    "prepublishOnly": "npm run check && npm test"
  },
  "engines": {
    "node": ">=20"
  }
}

{
  "name": "local-board",
  "version": "0.1.0",
  "description": "Repo-native Markdown ticket board for local agent workflows.",
  "type": "module",
  "license": "MIT",
  "bin": {
    "local-board": "./bin/local-board.js"
  },
  "scripts": {
    "check": "node --check bin/local-board.js && node --check src/cli.js && node --check src/config.js && node --check src/git.js && node --check src/scaffold.js && node --check src/tickets.js && node --check install.mjs",
    "test": "node --test",
    "validate": "node ./bin/local-board.js validate"
  },
  "engines": {
    "node": ">=20"
  }
}
